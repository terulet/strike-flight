# ONE MORE FLOOR

Vertical one-finger arcade prototype. Work in progress.

Run it:

```bash
cd games/one-more-floor
npm start        # http://localhost:5173
```

No dependencies, no build step, no install required (Node >= 20).
