# Build nativa (TestFlight). El frontend va dentro de la app, asi que las
# llamadas a la API necesitan una direccion absoluta, y compartir necesita
# saber que enlace web mandarle a quien todavia no tiene la app.
VITE_API_URL=https://mac-mini-de-eloi.tail011c69.ts.net
VITE_PUBLIC_URL=https://mac-mini-de-eloi.tail011c69.ts.net
VITE_NATIVE=1
